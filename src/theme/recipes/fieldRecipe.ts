import { defineRecipe } from "@chakra-ui/react";

const fieldRecipe = defineRecipe({
  base: {
    fontWeight: "medium",
    fontSize: "sm",
  },
  variants: {
    variant: {},
  },
});

export default fieldRecipe;
